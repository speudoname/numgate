# Nginx configuration with SSL and proper cookie handling
# This proxies requests to Vercel deployments

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name komunate.com www.komunate.com 104.248.51.150.nip.io 104.248.51.150;
    return 301 https://$host$request_uri;
}

# HTTPS Server
server {
    listen 443 ssl http2;
    server_name komunate.com www.komunate.com 104.248.51.150.nip.io 104.248.51.150;

    # SSL Certificate - using nip.io for now
    ssl_certificate /etc/letsencrypt/live/104.248.51.150.nip.io/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/104.248.51.150.nip.io/privkey.pem;
    
    # SSL Configuration
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;

    # Important: Set cookie path for all cookies to work across apps
    proxy_cookie_path ~*^/.* /;
    
    # Page builder app static assets (must come BEFORE main /_next)
    location ~ ^/page-builder/_next {
        proxy_pass https://pagenumgate.vercel.app;
        proxy_http_version 1.1;
        proxy_set_header Host pagenumgate.vercel.app;
        proxy_ssl_server_name on;
        proxy_cache_valid 200 60m;
        proxy_cache_bypass $http_cache_control;
        add_header X-Proxy-Cache $upstream_cache_status;
    }

    # Page builder API routes
    location ~ ^/page-builder/api {
        proxy_pass https://pagenumgate.vercel.app;
        proxy_http_version 1.1;
        proxy_set_header Host pagenumgate.vercel.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        
        # Pass cookies properly
        proxy_set_header Cookie $http_cookie;
        
        # Pass auth token from gateway cookie to page builder header
        set $auth_token "";
        if ($http_cookie ~* "auth-token=([^;]+)") {
            set $auth_token $1;
        }
        proxy_set_header x-auth-token $auth_token;
        
        proxy_ssl_server_name on;
    }

    # Page builder app (all other /page-builder paths)
    location /page-builder {
        proxy_pass https://pagenumgate.vercel.app;
        proxy_http_version 1.1;
        proxy_set_header Host pagenumgate.vercel.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        
        # Pass cookies properly
        proxy_set_header Cookie $http_cookie;
        
        # Pass auth token from gateway cookie to page builder header
        set $auth_token "";
        if ($http_cookie ~* "auth-token=([^;]+)") {
            set $auth_token $1;
        }
        proxy_set_header x-auth-token $auth_token;
        
        proxy_ssl_server_name on;
    }

    # Main gateway app static assets
    location /_next {
        proxy_pass https://numgate.vercel.app/_next;
        proxy_http_version 1.1;
        proxy_set_header Host numgate.vercel.app;
        proxy_ssl_server_name on;
        proxy_cache_valid 200 60m;
        proxy_cache_bypass $http_cache_control;
        add_header X-Proxy-Cache $upstream_cache_status;
    }

    # Main gateway API routes
    location /api {
        proxy_pass https://numgate.vercel.app/api;
        proxy_http_version 1.1;
        proxy_set_header Host numgate.vercel.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Cookie $http_cookie;
        proxy_ssl_server_name on;
    }

    # Main gateway app (everything else including /forgot-password)
    location / {
        proxy_pass https://numgate.vercel.app;
        proxy_http_version 1.1;
        proxy_set_header Host numgate.vercel.app;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header Cookie $http_cookie;
        proxy_ssl_server_name on;
    }
}

# Configuration for tenant custom domains (future)
# When tenants add custom domains, they will point them to our IP
# and we'll handle routing based on the Host header